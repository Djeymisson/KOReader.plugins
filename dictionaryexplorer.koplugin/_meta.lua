local _ = require("dictionaryexplorer_l10n")

return {
	fullname = _("Dictionary Explorer"),
	description = _("Adds a \"Go to dictionary\" button to the dictionary popup that opens the dictionary at the looked-up word, so you can read on through the neighbouring entries like a book."),
}
